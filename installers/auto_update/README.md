# Auto-update Scaffold

SubReparo auto-update is disabled by default.

Future auto-update must be:

- opt-in;
- signed;
- rollback-capable;
- approval-gated for major versions;
- disabled in enterprise policy unless explicitly enabled;
- verified in a disposable test environment before release.

Current status: policy scaffold only. No updater is installed or executed by this repository state.
